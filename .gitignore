# Bulk geodata never belongs in the repo (see CLAUDE.md -- lives in C:\geodata\ instead)
*.pmtiles
*.osm.pbf

# Node
node_modules/
npm-debug.log*

# Next.js
web/.next/
web/out/

# Env files (contain local paths / tokens)
web/.env.local
web/.env*.local

# Generated dev tool -- ~1.9MB, rebuilt wholesale from etl/cities.sqlite by the
# committed tools/build-city-overlay.js. Regenerate with `node tools/build-city-overlay.js`
# rather than carrying a new copy of it in history on every ETL rebuild.
tools/city-overlay.html

# Misc
*.log
.DS_Store
